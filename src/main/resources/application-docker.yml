spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/forgeflow
    username: forgeflow_app
    password: forgeflow_app

  flyway:
    url: jdbc:postgresql://postgres:5432/forgeflow
    user: forgeflow
    password: forgeflow

  data:
    redis:
      host: redis
      port: 6379

  kafka:
    bootstrap-servers: kafka:9092
